# Tana Input API Configuration
# Get your API token from https://app.tana.inc/?bundle=settings&panel=api

# Required: Your Tana API token
TANA_API_TOKEN=your_api_token_here

# Optional: Default target node (default: INBOX)
# Can be INBOX, SCHEMA, or a specific node ID
TANA_TARGET_NODE=INBOX

# Optional: API endpoint (default: official endpoint)
TANA_API_ENDPOINT=https://europe-west1-tagr-prod.cloudfunctions.net/addToNodeV2

# Export Feature Configuration
# Required for supertag-export token refresh functionality
# This is Tana's Firebase Web API key (client-side, not secret)
# Can be found by inspecting network traffic in the Tana app
TANA_FIREBASE_API_KEY=your_firebase_api_key_here
