{
  "name": "supertag-library-example",
  "version": "1.0.0",
  "description": "Example of using supertag-cli as a library in TypeScript applications",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "supertag-cli": "file:../.."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
